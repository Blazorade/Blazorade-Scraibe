# Bootstrap SCSS source downloaded by libman — regenerated by 'libman restore', do not commit
Styles/lib/

# LibSassBuilder intermediate output (compiled in-place by dotnet build)
Styles/app.css

# Compiled CSS, source map, and copied Bootstrap JS bundle — build outputs, do not commit
wwwroot/css/app.css
wwwroot/css/app.css.map
wwwroot/js/bootstrap.bundle.min.js
